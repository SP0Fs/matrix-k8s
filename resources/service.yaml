apiVersion: v1
kind: Service
metadata:
  name: matrix
  namespace: matrix
  labels:
    app: matrix
spec:
  ports:
    - port: 8008
      targetPort: http
      name: http
  selector:
    app: matrix
